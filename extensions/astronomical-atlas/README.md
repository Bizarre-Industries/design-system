# Astronomical Atlas extension

`DESIGN.md` is a byte-for-byte import of the approved Astronomical Atlas design authority recorded in `SOURCE.json`. Its classification is `approved-extension-with-overrides`.

The extension changes the canonical repository only where `DESIGN.md` is explicit. It governs Atlas, data, and material gradients; the interim Arabic stack and its public-shipping gates; and the breaking Workshop semantic-value replacements. The only non-negotiable identity invariants are the company name and Gravity Well geometry. Current tagline, type, theme, and layout decisions remain governed canonical decisions rather than immutable rules, and every topic not explicitly overridden falls back to the canonical repository.

The supplied ZIP is an approved source bundle with authority scoped by entry. Its `DESIGN.md` and source token decisions supersede the repository where explicit; the two corroborating Workshop values are an owner-approved override recorded in `governance/authority.json`. Invalid interchange shapes are translated into the canonical repository model, and conflicts inside the bundle resolve in favor of `DESIGN.md`. Generated HTML, CSS, PDF, mockups, validation reports, proposal imagery, Figma, Affinity, and Open Design outputs remain quarantined or derivative and never become authority merely because they were included in the archive.

See `governance/authority.json` for the machine-readable precedence contract.
