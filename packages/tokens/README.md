# @bizarre/tokens

This is the initial Bizarre design-token foundation package. It publishes canonical brand and theme-mode values, but is not yet a complete semantic-token or component-token contract.

Consumers must pin a released package version. Generated files are release artifacts and must not be edited directly.
